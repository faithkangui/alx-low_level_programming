#include "main.h"
/**
 * int _abs - computes the absolute valueee of an integer
 *
 *@a: int
 *
 * Return: int
 */
int _abs(int a)
{
	int n;
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
