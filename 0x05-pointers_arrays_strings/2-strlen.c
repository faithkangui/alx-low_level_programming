#include "main.h"
#include <stdlib.h>
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: char
 *
 * Return: int
 */
int _strlen(char *s)
{
	char i = *s;
	int len = strlen(i);

	return (len);
}
