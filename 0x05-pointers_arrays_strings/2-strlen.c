#include "main.h"
#include <stdlib.h>
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: char
 *
 * Return: int
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
