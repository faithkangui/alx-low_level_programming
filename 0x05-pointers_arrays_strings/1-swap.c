#include "main.h"
#include <stdio.h>
/**
 * swap_int - swaps the values of two integers
 * @a: integer int to be swaped
 * @b: integer int to be swaped
 *
 * Return: void
 */
void swap_int(int *a, int *b)
{
	int a, int b = *p1 = &a, *p2 = &b;

	*p1 = *p1 + *p2;
	*p2 = *p1 - *p2;
	*p1 = *p1 - *p2;
}
