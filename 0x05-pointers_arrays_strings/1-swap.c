#include "main.h"
#include <stdio.h>
/**
 * swap_int - swaps the values of two integers
 * @*a - integer
 * @*b - integer
 *
 * Return: void
 */
void swap_int(int *a, int *b)
{
	int temp;
	int *a;
	int *b;

	temp = x;
	*a = *b;
	*b = temp;
	_putchar(*a);
	_putchar(*b);
	_putchar('\n')
}
