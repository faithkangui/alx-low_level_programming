#include "main.h"
#include <stddef.h>
/**
 * _strchr - locates a string of character.
 * @s: pointer to string
 * @c: char to check
 *
 * Return: pointer to char c or NULL char not found
 */
char *_strchr(char *s, char c)
{
	while (*s != c)
		s++;
	return (s);
	while (c == '\0')
	{
	return (NULL);
	}
}
